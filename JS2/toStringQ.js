const obj = {
    name: "Shubham",
    // toString() {
    //     return Object.getPrototypeOf(this).toString();
    // }
}
console.log(obj.toString());

// conclusion: when object is printed in console log. Its not to String method that does 
// the job of printing.