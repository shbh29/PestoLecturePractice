// Promise.resolve("abc").then(console.log);

// let p = Promise.resolve({a: "Shivani"});

// console.log(p);

// console.log(new Promise(()=>{}));

// let obj = {
//     then(resolve, reject) {
//         setTimeout(resolve, 1000);
//     }
// };

// Promise.resolve(obj).then(() => console.log("method resolved after one second"));